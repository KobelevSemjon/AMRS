#include "graphfeatures.h"
/*
GraphFeatures::GraphFeatures()
{

}

GraphFeatures::GraphFeatures(const std::map<cv::Vec2i, int> &graph_link_list)
{
    rebuild(graph_link_list);
}

void GraphFeatures::rebuild(const std::map<cv::Vec2i, int> &graph_link_list)
{
    using namespace std;
    using namespace cv;
    _graph_link_list.clear();
    _graph_link_list.reserve(graph_link_list.size());
    for(const auto& link: graph_link_list)
    {
        Vec3i record(link.first[0],link.first[1],link.second);
        _graph_link_list.push_back(record);
    }
}*/
